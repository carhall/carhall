<%= index_for @activities, class: :datatable do |t| %>
  <%= t.attribute :id %>
  <%= t.attribute :image do |image| %>
    <%= image_tag(image.url(:thumb)) %>
  <% end %>
  <%= t.attribute :title %>
  <%= t.attribute :expire_at, format: :short %>
  <%= t.actions :all %>
<% end %>
