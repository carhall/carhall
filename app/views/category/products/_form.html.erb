<%= simple_form_for(@product, html: { class: 'form-horizontal' }) do |f| %>
  <%= devise_error_messages! @product %>

  <%= f.input :name, autofocus: true %>

  <%= f.button :submit, class: 'btn-submit' %>
<% end %>