<%= simple_form_for(@brand, html: { class: 'form-horizontal' }) do |f| %>
  <%= devise_error_messages! @brand %>

  <%= f.input :name, autofocus: true %>

  <%= f.button :submit, class: 'btn-submit' %>
<% end %>