<% if @user_type == :admin %>
  <% content_for :title do %>
    后台管理
  <% end %>
<% end %>

<!DOCTYPE html>
<html>
  <%= render 'shared/head' %>
  <body>
    <div class="container">
      <%= render 'shared/header' %>

      <div class="navbar">
        <div class="navbar-inner">
          <div class="container">
            <% case @user_type %>
            <% when :provider, :dealer, :admin %>
              <%= render "shared/nav/#{@user_type}" %>
            <% else %>
              <%= render 'shared/nav/welcome' %>
            <% end %>
          </div>
        </div>
      </div><!-- /.navbar -->

      <%= render 'shared/notices' %>

      <!-- Container -->
      <%= yield %>
      
      <%= render 'shared/footer' %>
    </div> <!-- /container -->
    <%= render 'shared/javascripts' %>
  </body>
</html>
